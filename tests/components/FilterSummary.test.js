describe("AccountDetail", () => {
  describe("dom", () => {
    describe(".filter_title", () => {
      test.todo("renders filter name")

      describe("filter disabled", () => {
        test.todo("renders (Disabled)")
      })
    })

    describe("template filter", () => {
      describe(".template_name", () => {
        test.todo("render filter template name")
      })

      test.todo("render filter params")
    })

    test.todo("renders filter expression")
  })

  describe("computed", () => {
    describe("params", () => {
      describe("not template filter", () => {
        test.todo("is []")
      })

      test.todo("is array of template param name : filter param")
    })
  })
})
