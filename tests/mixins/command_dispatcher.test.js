describe("command_dispatcher", () => {
  describe("#created", () => {
    test.todo("sets cmd_id to 0")
  })

  describe("methods", () => {
    describe("#sendCmd", () => {
      test.todo("increments cmd_id")
      test.todo("sets id on command")
      test.todo("sends command object via websocket")

      describe("callback specified", () => {
        test.todo("registers callback with id")
      })
    })
  })
})
