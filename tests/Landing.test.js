describe("Landing Page", () => {
  describe("computed", () => {
    describe("#blockchain_text", () => {
      describe("no blockchain configured", () => {
        test.todo("is 'Blockchain'")
      })

      describe("blockchain configured", () => {
        test.todo("is configured_blockchain_upper")
      })
    })
  })

  describe("methods", () => {
    describe("#img_src", () => {
      describe("no blockchain configured", () => {
        test.todo("is /assets/landing/blockchain/N.png")
      })

      describe("blockchain configured", () => {
        test.todo("is /assets/landing/configured_blockchain/N.png")
      })
    })
  })
})
