describe("Notification Page", () => {
  describe("dom", () => {
    test.todo("renders notification filter matches")
  })

  describe("#created", () => {
    describe("MAINTENANCE_MODE", () => {
      test.todo("redirects to maintenance page")
    })

    test.todo("loads notification")

    describe("notification response", () => {
      test.todo("transforms and sets filter matches")
    })
  })
})
